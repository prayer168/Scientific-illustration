# v1.0.0 測試報告

測試日期：2026-07-19

## 自動檢查

- `node scripts/validate.mjs`：通過。確認 12 個單元、28 個頁面圖像標籤、10 題測驗、19 筆已註記查核日期的資源，以及 41 個本機路徑。
- `node --check`：`app.js`、`quiz.js`、`records.js` 全數通過語法檢查。
- JSON 解析：`project.config.json`、`data/quiz.json`、`data/resources.json` 通過。
- Lighthouse（本機伺服器）：Accessibility 100、Best Practices 100、SEO 100，沒有計分項目失敗。完整機器可讀報告為 `docs/lighthouse-final.json`。

## 瀏覽器情境

- 桌機 1440 × 1000：12 單元分頁、作品篩選與放大對話框、深色模式皆正常；頁面無水平溢位。
- 手機 390 × 844：折疊式單元選單、主要操作、固定上／下一單元按鈕正常；頁面無水平溢位。
- 測驗：開始挑戰、單選送出、鎖定答案、正確回饋與下一題按鈕正常。
- 學習紀錄：輸入作品名稱與觀察地點、勾選檢核項目後重新載入，資料仍保留。
- 作品欣賞：點選圖片後對話框開啟，Esc 可關閉。
- 空白學習單：獨立頁面可載入，標題正確。
- 離線入口：以 `file://` 直接開啟 `index.html`，12 單元與腳本正常載入，沒有主控台錯誤。

## 可及性人工檢查

- 有跳到主要內容連結、語意化標題、分頁角色、進度列名稱與即時回饋區。
- 圖像均有替代文字；動態圖庫圖像會同步替代文字。
- 手機隱藏可見按鈕文字時，主題與全螢幕按鈕仍有動態 `aria-label`。
- 支援鍵盤焦點樣式、`prefers-reduced-motion`、深色模式與列印樣式。

## 已知限制

- 使用者資料只儲存在目前瀏覽器的 LocalStorage，清除瀏覽資料或按「重新開始」會移除。
- 全螢幕功能受瀏覽器與裝置權限限制；被拒絕時不影響其他教材功能。
- 外部資源網站可能調整路徑或阻擋自動化連線，正式授課前宜抽查常用連結。
